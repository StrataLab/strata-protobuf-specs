name: plasma_protobuf
description: PlasmaLaboratories proto definitions, including model types and gRPC services.

version: 0.1.1

homepage: "https://plasmalabs.org"
repository: "https://github.com/PlasmaLaboratories/plasma-protobuf-specs"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  protobuf: ^3.0.0
  grpc: ^4.0.1
  fixnum: ^1.1.0

dev_dependencies:
  test: ^1.24.3
