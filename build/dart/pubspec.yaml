name: strata_protobuf
description: Stratalab proto definitions

publish_to: 'none'

version: 0.1.0+1

environment:
  sdk: ">=3.0.0"

dependencies:
  protobuf: ^3.0.0
  grpc: ^3.2.2

dev_dependencies:
  test: ^1.24.3
